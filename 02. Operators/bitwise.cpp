#include<iostream>

using namespace std;
int main(){

    // bitwise operator

    // and (&)
    // or (|)
    // xor (^)
    // negation (~)
    // right shift (>>)
    // left shift (<<)

    int a=10;
    int b=9;

    cout<<(a&b);

}